name: payparq_scanner
description: Tactical Mobile Scanner for PayParq.ai
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5
  # google_mlkit_text_recognition: ^0.11.0 # Temporarily disabled for Web compatibility
  supabase_flutter: ^1.10.0
  # provider: ^6.0.0 # Redundant with Riverpod
  google_fonts: ^6.1.0
  image_picker: ^1.0.7
  flutter_riverpod: ^2.6.1
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
