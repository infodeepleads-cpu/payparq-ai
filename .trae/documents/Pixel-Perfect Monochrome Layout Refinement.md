I will now implement the exact color scheme and layout from your reference photo:

1. **Theme Update**: 
   - Define `sidebarBackground` as `#333333` (the dark gray from the photo).
   - Define `headerBackground` as `#000000` (the pure black).

2. **Scaffold Redesign**:
   - Wrap the `MasterScaffold` body in a `Column`.
   - Add a top `Container` (height ~64) with `headerBackground` (Black) for the logo and Deploy button.
   - Place the `Row` (Sidebar + Content) below it.
   - Set the `Sidebar` background to `sidebarBackground` (Gray).

3. **Logo & Text**:
   - Ensure 'payparq.ai' and sidebar icons remain perfectly visible against their new backgrounds.

Shall I execute these changes now?